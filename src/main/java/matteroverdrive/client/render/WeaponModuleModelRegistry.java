
package matteroverdrive.client.render;

public class WeaponModuleModelRegistry {
	/*
	 * Map<String,WavefrontObject> models;
	 * 
	 * public WeaponModuleModelRegistry() { models = new HashMap<>(); }
	 * 
	 * public void registerModule(IWeaponModule module) { IModelCustom m =
	 * AdvancedModelLoader.loadModel(new ResourceLocation(module.getModelPath()));
	 * if (m instanceof WavefrontObject) {
	 * models.put(module.getModelPath(),(WavefrontObject)m); } }
	 * 
	 * public WavefrontObject getModel(String model) { return models.get(model); }
	 */
}
